[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uvicorn scanning_api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/materials/barcode-lookup?barcode=test"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
